{% extends 'base.html.twig' %}

{% block title %}Espace administrateur - La boutique design{% endblock %}

{% block body %}
<header id="fh5co-header" class="fh5co-cover js-fullheight" role="banner" style="max-height:35vh; background-image: url('../../../assets/images/main-bg-admin.jpg')" data-stellar-background-ratio="0.5">
  <div class="overlay"></div>
  <div class="container container-small">
    <div class="row">
      <div class="col-md-12 text-center display-tc">
        <h1>Espace administrateur</h1>
      </div>
    </div>
  </div>
</header>
<div class="container" style="margin-top: 1rem">
  <a href="{{ path('admin_category_list') }}" class="btn-success btn-lg" role="button" style="float:right; margin:2rem">
    <i class="icon-back"></i>&nbsp;RETOUR LISTE
  </a>
  <div class="row" style="clear:both; display:flex; justify-content:center;">
    <div class="col-xs-12 col-sm-8 col-md-6 form-container">
      <h2 class="text-center" style="color:white;margin-top: 1rem">Modifier la catégorie {{ category.name }}</h2>
      <div class="form-goup">
        {{ form_start(categoryForm) }}
        <div class="form-group">
          {{ form_row(categoryForm.name) }}
        </div>
        <div class="form-group" style="display: flex; justify-content: space-between;">
          <button type="submit" class="btn btn-primary btn-outline btn-lg">Enregistrer les modifications</button>
        </div>
        {{ form_end(categoryForm) }}
      </div>
    </div>
  </div>
</div>
{% endblock %}
